node_modules/
.env
.env.*
!.env.sample
.acommit/

# OS / editor junk
.DS_Store
**/.DS_Store
Thumbs.db

# Test / tooling output
coverage/
*.log

# Build output (rebuilt via npm run build:web / prepublishOnly)
dist/

# Rules UI dev deps
web/node_modules/

# Local port manager (generated per machine)
scripts/port.sh

# Internal — local experiments only (not GitHub, not npm)
experiments/
examples/screenshot-demo/
docs/
.claude/
.acommit/results/
