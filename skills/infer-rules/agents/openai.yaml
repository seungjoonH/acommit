interface:
  display_name: "acommit Infer Rules"
  short_description: "Infer acommit rules from repository Git history"
  default_prompt: "Use $infer-rules to analyze Git history and propose a rules.yml file."
